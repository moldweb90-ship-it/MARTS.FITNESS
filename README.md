# MARTS FITNESS Landing

Запуск локально:

```bash
npm install
npm run dev -- --host --port 5000
```

Продакшен-сборка:

```bash
npm run build
npm run preview -- --host --port 5000
```

Код стартовой страницы лежит в `src/App.jsx`. Tailwind подключён через `src/index.css`.

